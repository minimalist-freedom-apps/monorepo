## Verification

Always verify your work using these commands and **only** these commands:

1. **TypeScript** — `bun typecheck`
2. **Tests** — `bun test`

Do **not** run `npx tsc`, `npx vitest`, or any other variations.
