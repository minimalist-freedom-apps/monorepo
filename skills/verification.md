## Verification

Always verify your work using these commands and **only** these commands:

1. **TypeScript** — `pnpm typecheck`
2. **Tests** — `pnpm test`

Do **not** run `npx tsc`, `node --test`, or any other variations.
