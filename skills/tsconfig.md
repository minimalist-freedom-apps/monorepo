## tsconfig
- The `tsconfig.json` files in each package extend the base configuration from `@minimalist-apps/tsconfig`.
- Every additional configuration must be documented with comments in the respective `tsconfig.json` files.
- `"jsx": "react-jsx"` does not require a comment — its purpose is obvious when the package contains React components.