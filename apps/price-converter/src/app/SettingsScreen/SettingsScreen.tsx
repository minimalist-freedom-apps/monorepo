import { Title } from '@minimalistic-apps/components';

export const SettingsScreen = () => {
    return (
        <div style={{ padding: 24 }}>
            <Title level={2}>Settings</Title>
        </div>
    );
};
